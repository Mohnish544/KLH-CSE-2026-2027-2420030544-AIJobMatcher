# Data

This folder holds sample/reference data used by the project (e.g., sample resumes,
sample job descriptions used to test the matching engine), or a reference file
pointing to an external data source if the data isn't stored directly in the repo.

Add your sample data files here, or replace this file with a DATA_SOURCE.md
describing where the data comes from (e.g., a public dataset link).
